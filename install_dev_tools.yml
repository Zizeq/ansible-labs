#Jakub Dykier 236641
- name: Configure the developer environment (Node.js and .NET)
  hosts: app_servers
  become: yes

  tasks:
    - name: Install required runtime environments
      apt:
        name:
          - apt-transport-https
          - nodejs
          - npm
          - redis-server
          - rabbitmq-server
          - postgresql
          - postgresql-contrib
        state: present
        update_cache: yes

    - name: Step 1 - Download the Microsoft repository configuration (for Ubuntu 22.04)
      get_url:
        url: https://packages.microsoft.com/config/ubuntu/22.04/packages-microsoft-prod.deb
        dest: /tmp/packages-microsoft-prod.deb

    - name: Step 4 - Download .NET
      get_url:
        url: https://dot.net/v1/dotnet-install.sh
        dest: /tmp/dotnet-install.sh
        mode: '0755'

    - name: Step 5 - Run script and instalation .NET 10
      command: /tmp/dotnet-install.sh --channel 10.0 --install-dir /usr/share/dotnet
      args:
        creates: /usr/share/dotnet/dotnet
